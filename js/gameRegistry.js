/**
 * js/gameRegistry.js — LEGACY (kept for reference only)
 *
 * All games have been migrated to GameRegistry.register() in their
 * own JS files. This file is no longer used at runtime and can be
 * deleted once confirmed stable.
 *
 * The legacy window.GAMES array that used to live here has been
 * superseded by js/core/gameRegistry.js.
 */
